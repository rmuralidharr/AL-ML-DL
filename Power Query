Power query Excel

files load .csv,.xlsx,


transformation  
	basic -filter, data type change ,remove columns,
	Advanced - calculation, merge,append & group by
	
cleanse
	 clean dirty data,column profiling tools,
Connect to data source
	connect to a wide range
	
transformation
data types,remove column,change filed names,Replace value,Change Text case- Upper ,Lower and proper,create a new filed 


Advanced Transformation

Merge, Append data to existing data ,custom calculation, group by option 


Merge possible one way only a->b, a<-b  but not a-><-b not possible

Append data in excel

custom column calculation,

group by calculation

===================cleaning data=========================
learn  to clean data
Prepare data  for analysis
Column profile tools

----------Power query diagnostic-----------
view -- show whitespace,column quality, column distribution , column profile

-------clean-------
home -> remove row , use first row s as header


power pivot need license  (available in menu bar)    

========= Loading the data source=======
Azure SQl server
CSV\TXT
Web tables
xml & json tables
Google sheets
Excel tables


*******************Advanced Excel Power Query-M language, troubleshoot, Pattern*******************
Troubleshooting, Case study, M language, Power query pattern 

-------Errors while adding / deleting / renaming columns-----

if any main source data change it will reflect on main power query but not subsequent power queries so u need to change the 

M language is case sensitive 

f(x)= Table.ColumnNames(Source) we get table column names

f(x)= Table.ColumnNames(Source){0} it will retun 1 st value of column names

-------------Handling Null values during addition / Multiplication / Division--------------

if [Dates.2] <> null then
{Number.From([Leave Date.1])..Number.From([Leave Date.2])}
else null

List.Dates([Start Date],Duration.Days([End Date]-[Start Date]),#duration(1,0,0,0))

IF([@name]=F1,SUM(H1,1),1)


=======advances Editor=======

let 
 Source=
in 
 #"wow"
 
 
if incase  u want without #"" with starting name operation no spaces in operation names


grouping queries 
Grouping Queries and Query Dependency view


Ageing Bucket analysis



DateTime.Date(DateTime.LocalNow())

Current Date
= DateTime.Date(DateTime.LocalNow()) current date


